name: Playwright Tests

on:
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.59.1-noble
      options: --ipc=host
    timeout-minutes: 10
    strategy:
      fail-fast: true
      matrix:
        browser: [chromium]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Cache npm packages
        uses: actions/cache@v5
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Install dependencies
        run: npm ci

      - name: Install app dependencies
        run: npm ci --prefix app

      - name: Start app
        run: cd app && npm start &

      - name: Wait for app
        run: |
          timeout 10s bash -c 'until curl -fsS http://localhost:3000/api/v1/healthcheck >/dev/null 2>&1; do sleep 1; done' || { echo "app failed to become ready in time" >&2; exit 1; }
          echo "app is ready"

      - name: Run tests
        run: npx playwright test --project=${{ matrix.browser }}

      - name: Upload test failures artifact
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: test-failures-${{ matrix.browser }}
          path: test-results/failures.json
          retention-days: 1

      - name: Upload Playwright report
        if: ${{ success() || failure() }}
        uses: actions/upload-artifact@v6
        with:
          name: playwright-report-${{ matrix.browser }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: playwright-report/
          retention-days: 14
