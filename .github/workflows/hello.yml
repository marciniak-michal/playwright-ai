name: Hello CI

on:
  push:
    branches:
      - feat/github-actions

jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Print hello message
        run: echo "Hello from CI"
